'use strict'

// const converter = (evro) => {
//   return evro * 87.6
// }

// console.log (converter(2));

const converter = (evro) => {
  return alert(evro * 87.6)
}

const mat = converter( Number(prompt('Введите стоимость покупки в евро и получите вывод в рублях: ')));